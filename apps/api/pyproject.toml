[project]
name = "clean-e-yay-api"
version = "2.0.0"
description = "FastAPI HTTP layer for Clean E-yAy"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.6",
  "pyyaml>=6.0",
]
